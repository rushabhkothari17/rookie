<analysis>**original_problem_statement:**
The user has reported a critical regression of a cross-tenant data leak, where tenants are able to see products, articles, and categories belonging to other tenants. This is the top priority.

In addition, the user has requested a significant batch of new features primarily for the  role:
1.  **Dual Impersonation:** A new customer switcher dropdown to allow platform admins to impersonate customers of a specific tenant, in addition to the existing tenant switcher. This should include the ability to switch back from a customer view to the tenant admin view.
2.  **Tenant Switcher UI/UX:**
    *   Implement a search bar within the tenant switcher dropdown to filter a potentially long list of tenants.
    *   By default, the dropdown should only show the top 5 tenants.
3.  **Setup Checklist Widget:** Implement a new widget on the admin dashboard to guide new tenants through the initial setup process.
4.  **Business Rule Enforcement:** A tenant can only have one user with the  role.

The user has also requested confirmation that the tenant switcher feature is exclusively available to the platform admin.

**User's preferred language**: English

**what currently exists?**
The application is a multi-tenant SaaS platform built with FastAPI and React. It features a  role that can impersonate other tenants using a tenant switcher in the navigation bar, which sends an  header. The application recently underwent a massive bug-fixing effort to eliminate widespread data leaks, culminating in a successful pass (18/18 tests) from the testing agent. This effort included implementing Python's  to automatically inject the  into all audit log entries.

However, a regression or a new variant of the data leak has been discovered. The previous agent identified the root cause: several frontend admin tabs (, ) were incorrectly calling public-facing API endpoints (e.g., ) instead of the correct tenant-aware admin endpoints (e.g., ). Public endpoints do not respect the  header, causing the platform admin to see data from the default tenant instead of the impersonated tenant.

**Last working item**:
    - Last item agent was working: The agent was actively investigating the new P0 data leak. It had correctly diagnosed that frontend admin components were using the wrong APIs and had started planning the implementation for all the new user-requested features (dual switcher, setup checklist, etc.) after viewing all necessary files.
    - Status: IN PROGRESS
    - Agent Testing Done: N
    - Which testing method agent to use? both. Fixing the data leak and implementing the new dual-switcher system are significant changes that require a full regression test using  for both frontend and backend.
    - User Testing Done: N

**All Pending/In progress Issue list**:
  - Issue 1: **Critical Cross-Tenant Data Leak Recurrence (P0)**

  Issues Detail:
  - Issue 1: 
     - Attempted fixes: The agent performed a comprehensive investigation and found that frontend admin tabs like  and  were calling public APIs, which ignore the  impersonation header. The fix is to change these calls to use the corresponding  endpoints.
     - Next debug checklist:
        1.  Modify  to fetch data from  instead of .
        2.  Modify  to fetch article categories from .
        3.  Perform a quick audit of all other admin tab components to ensure they exclusively use  endpoints for data fetching.
        4.  Test by logging in as the , switching to , and verifying that the Products and Articles tabs show only data belonging to .
     - Why fix this issue and what will be achieved with the fix? This is a critical data breach that violates the core principle of the multi-tenant architecture. Fixing it is essential to ensure tenant data is secure and isolated.
     - Status:  IN PROGRESS
     - Is recurring issue? Y
     - Should Test frontend/backend/both after fix? Both
     - Blocked on other issue: None. This is the top priority blocker.

**In progress Task List**:
- None. The previous tasks were completed, and the current work is categorized as a P0 issue and new upcoming tasks.

**Upcoming and Future Tasks**
**Upcoming Tasks:**
*   **(P0) Implement Dual Impersonation Switcher:**
    *   Create a new Customer Switcher dropdown in the  component, visible only to platform admins.
    *   Create a new backend endpoint (e.g., ) to fetch a list of customers for the selected tenant.
    *   Update backend dependencies to handle a new  header to serve the correct customer portal view.
*   **(P0) Enhance Tenant Switcher UI:**
    *   Add a search input field to the  component to filter tenants.
    -   Limit the initial display to the top 5 tenants.
*   **(P1) Implement Setup Checklist Widget:**
    *   Design and implement a new React component for the admin dashboard that tracks and displays the completion status of key setup tasks (e.g., brand customization, first product added).
    *   This will likely require new backend models and endpoints to store and retrieve the checklist state for each tenant.
*   **(P1) Enforce Single Super Admin Rule:**
    *   Add validation logic to the user creation and update endpoints () to prevent assigning the  role if one already exists for the given tenant.

**Completed work in this session**
- **Critical Bug Fixes (Session-wide):**
  - **Solved Incomplete Audit Logs:** Architecturally solved a systemic issue by using  to automatically inject  into all audit log calls from FastAPI dependencies (, ), eliminating the need for hundreds of manual code changes.
  - **Fixed Admin Permission Bypasses:** Resolved multiple bugs where admin checks were incorrectly bypassing tenant data isolation rules, particularly in the articles module.
  - **Fixed Partner Orgs Tab Visibility:** Implemented a reactive fix using a listener pattern () in  to correctly hide the tab when a platform admin impersonates another tenant.
  - **Fixed React Hydration Error:** Resolved a hydration warning by moving a  read from the module level to a  hook in .
- **Feature Implementation & Enhancement:**
  - **Implemented Tenant Seed Data:** Created a  function in  to provision new tenants with a full set of generic sample data, providing a complete out-of-the-box experience.
  - **Implemented Payment Fallback:** Added a Request a Quote button to the product detail and cart pages, which appears for tenants who have not configured any payment integrations.
- **Testing and Verification:**
  - Passed a comprehensive regression test suite (18/18 tests passed in ) from the , confirming the stability of the application *before* the latest user report.

**Earlier issues found/mentioned but not fixed**
- All issues from the previous handoff were addressed. The current pending work is based on the most recent user feedback.

**Known issue recurrence from previous fork**
  - **Issue recurrence in previous fork**: Critical Data Leaks between tenants.
  - **Recurrence count**: 3+
  - **Status**: IN PROGRESS. A new variant was discovered where admin frontend components were using public APIs. This is the current P0 priority.

**Code Architecture**


**Key Technical Concepts**
- **Multi-Tenancy Data Leak (Regression):** The core problem is ensuring all data access points, especially admin UI components, use the correct tenant-aware APIs and respect the  header for impersonation.
- **Dual Impersonation:** The upcoming task requires a two-layer impersonation model (Admin -> Tenant -> Customer), which will likely involve a new  header and updated backend logic to manage this layered context.
- **Context Variables:** The use of Python's  is a key architectural pattern in this codebase for implicitly passing request-scoped data like  to services without modifying function signatures.
- **Reactive Frontend State:** Using a custom listener/subscriber pattern to allow loosely coupled React components to communicate state changes (e.g.,  notifying ).

**key DB schema**
- No schema changes. All work involves correctly querying existing collections using the  field.

**changes in tech stack**
- None.

**All files of reference**
- **Files to Fix Immediately (P0 Data Leak):**
  - 
  - 
- **Files for New Features:**
  - 
  - 
  - 
  - 
  - 

**key api endpoints**
- **Endpoints to Fix Usage Of:**
  - 
  - 
- **Endpoints to Create:**
  -  (for the customer switcher)
  - Endpoints to manage the state of the Setup Checklist widget.

**Critical Info for New Agent**
1.  **PRIORITY #1: FIX THE P0 DATA LEAK.** The user has reported a critical regression. Your first and only immediate task is to fix the identified issue where admin UI tabs are using public APIs. Modify  and  to use their respective  endpoints. Do not proceed to new features until this leak is patched and verified.
2.  **PLAN THE NEW FEATURES CAREFULLY:** After fixing the leak, your next task is to implement the batch of new features. The dual-switcher (Tenant + Customer) is the most complex. Plan how the frontend state and backend headers (, ) will interact.
3.  **ENFORCE BUSINESS RULES:** When you implement the user management changes, remember to add backend validation to enforce the one super admin per tenant rule.
4.  **RE-TEST EVERYTHING:** Because a data leak regression occurred after a full test pass, you must be extra diligent. After implementing all fixes and new features, run the  with a comprehensive plan that explicitly tests the new data leak scenario and the new impersonation features.

**documents and test reports created in this job**
-  (Updated)
-  (Passed 18/18 tests but is now outdated by the new bug report)

**Last 10 User Messages and any pending HUMAN messages**
- **User (latest):** Reported a data leak regression (tenants seeing each other's data). Requested a new customer switcher, search functionality in the tenant switcher, approved the Setup Checklist idea, and specified the one super admin per tenant rule.
- **Agent:** Finished a successful testing agent run, updated the PRD, and was about to conclude the task.
- **User:** Reported a new bug where Tenant B couldn't edit an article (failed to load). Also requested P2 tasks be completed and that audit trails be comprehensively implemented.
- **Agent:** Investigated and fixed the article loading bug, a React hydration warning, and the payment fallback logic. Critically, enhanced the audit log system to cover all routes via .
- **User:** Reported three issues after the agent's major bug bash: Partner Orgs tab was still visible, Tenant B couldn't edit articles, and audit logs were missing for tenants.
- **Agent:** Investigated and fixed all three issues with a mix of frontend state management fixes and backend permission corrections.

**Project Health Check:**
- **Broken:** A critical P0 data leak regression has been reported. The application is insecure and not functional for its primary purpose until this is fixed.

**3rd Party Integrations**
- Stripe
- GoCardless
- Resend
- Tiptap

**Testing status**
  - Testing agent used after significant changes: YES, however, the last successful run () did not catch the newly reported data leak regression.
  - Troubleshoot agent used after agent stuck in loop: NO
  - Test files created: []
  - Known regressions: A critical cross-tenant data leak (P0) is active.

**Credentials to test flow:**
- **Platform Admin**:  / 
- **Partner Code for default tenant**: 
- **Tenant B Admin**:  / 
- **Partner Code for Tenant B**: 

**What agent forgot to execute**
- The agent was interrupted just before implementing the fix for the P0 data leak it had correctly diagnosed. The implementation of this fix is the immediate next step.
- All new feature requests from the user's latest message are pending implementation.</analysis>
